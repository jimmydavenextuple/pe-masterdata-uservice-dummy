jar {
    enabled = true
}
bootJar {
    enabled = false
}

dependencies {
    implementation(project(":common-util"))
    implementation(project(":nfs-common:nfs-common-persistence"))
    implementation(project(":nfs-common:nfs-common-cache"))
}