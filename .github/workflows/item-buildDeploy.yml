name: ITEM BUILD-DEPLOY

on:
  push:
    branches: [ devr3 ]
    paths:
      - "item/**"
      - "node/**"
      - "deploy-scripts/**"
      - "manifests/**"
      - ".github/**"
     
jobs:
  Build-Deploy:
    name: R3
    uses: ./.github/workflows/build&deploy.yml
    with:
      module: item
    secrets: inherit




