name: GLOBAL-CONFIGURATION BUILD-DEPLOY

on:
  push:
    branches: [ development ]
    paths:
      - "global-configuration/**"
      - "deploy-scripts/**"
      - "manifests/**"
      - ".github/**"
jobs:

  Build-Deploy:
    uses: ./.github/workflows/build&deploy.yml
    with:
      module: global-configuration
    secrets: inherit




