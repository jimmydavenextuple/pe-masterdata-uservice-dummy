name: WEIGHTAGE-CONFIGURATION HOTFIX

on:
  push:
    branches: [ hotfix ]
    paths:
      - "weightage-configuration/**"
      - "deploy-scripts/**"
      - "manifests/**"
      - ".github/**"
      - "common-util/**"
      - "build.gradle"
jobs:

  Build-Deploy:
    uses: ./.github/workflows/Build&Deploy_Hotfix.yml
    with:
      module: weightage-configuration
    secrets: inherit




