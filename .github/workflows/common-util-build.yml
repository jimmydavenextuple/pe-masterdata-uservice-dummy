name: COMMON-UTIL BUILD

on:
  push:
    branches: [ development ]
    paths:
      - "common-util/**"
jobs:

  Build-Publish:
    uses: ./.github/workflows/build.yml
    with:
      module: common-util
    secrets: inherit




