name: COMMON-UTIL BUILD

on:
  push:
    branches: [ development, hotfix ]
    paths:
      - "common-util/**"
      - ".github/workflows/common-util-build.yml"
jobs:

  Build-Publish:
    uses: ./.github/workflows/build.yml
    with:
      module: common-util
    secrets: inherit




