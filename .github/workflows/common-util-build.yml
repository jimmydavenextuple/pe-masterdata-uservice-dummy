name: COMMON-UTIL BUILD

on:
  push:
    branches: [ devr3, hotfix ]
    paths:
      - "common-util/**"
jobs:

  Build-Publish:
    name: R3
    uses: ./.github/workflows/build.yml
    with:
      module: common-util
    secrets: inherit




