name: NODE-CARRIER HOTFIX

on:
  push:
    branches: [ hotfix ]
    paths:
      - "node-carrier/**"
      - "deploy-scripts/**"
      - "manifests/**"
      - ".github/**"
      - "common-util/**"
      - "build.gradle"
     
jobs:
  Build-Deploy:
    uses: ./.github/workflows/Build&Deploy_Hotfix.yml
    with:
      module: node-carrier
    secrets: inherit




