name: COMMON BUILD

on:
  push:
    branches: [ devr3, hotfix ]
    paths:
      - "common/**"
      - "manifests/**"
jobs:

  Build-Publish:
    name: R3
    uses: ./.github/workflows/build.yml
    with:
      module: common
    secrets: inherit




