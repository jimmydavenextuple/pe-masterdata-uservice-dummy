name: COMMON BUILD

on:
  push:
    branches: [ development, hotfix ]
    paths:
      - "common/**"
      - "manifests/**"
      - "common-util/**"
      - "build.gradle"
jobs:

  Build-Publish:
    uses: ./.github/workflows/build.yml
    with:
      module: common
    secrets: inherit




