name: NFS-NEAR-CACHE BUILD

on:
  push:
    branches: [ development, hotfix ]
    paths:
      - "nfs-near-cache/**"
      - ".github/workflows/nfs-near-cache-build.yml"
jobs:

  Build-Publish:
    uses: ./.github/workflows/build.yml
    with:
      module: nfs-near-cache
    secrets: inherit




