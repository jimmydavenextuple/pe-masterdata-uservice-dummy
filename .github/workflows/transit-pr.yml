name: PR-TRANSIT

on:
  workflow_call:
  pull_request:
    paths:
      - "transit/**"
    branches: [ devr3, hotfix ]

jobs:
  PR-Analysis:
    name: R3
    uses: ./.github/workflows/pr-flow.yml
    with:
      module: transit
    secrets: inherit