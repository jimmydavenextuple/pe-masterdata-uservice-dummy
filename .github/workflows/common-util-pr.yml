name: PR-COMMON-UTIL

on:
  workflow_call:
  pull_request:
    paths:
      - "common-util/**"
      -  ".github/workflows/common-util-pr.yml"
    branches: [ devr3, hotfix ]

jobs:
  PR-Analysis:
    name: R3
    uses: ./.github/workflows/pr-flow.yml
    with:
      module: common-util
    secrets: inherit