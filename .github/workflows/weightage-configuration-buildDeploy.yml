name: WEIGHTAGE-CONFIGURATION BUILD-DEPLOY

on:
  push:
    branches: [ devr3 ]
    paths:
      - "weightage-configuration/**"
      - "deploy-scripts/**"
      - "manifests/**"
      - ".github/**"
jobs:

  Build-Deploy:
    name: R3
    uses: ./.github/workflows/build&deploy.yml
    with:
      module: weightage-configuration
    secrets: inherit




