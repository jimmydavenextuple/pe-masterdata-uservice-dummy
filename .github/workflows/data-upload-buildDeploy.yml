name: DATA-UPLOAD BUILD-DEPLOY

on:
  push:
    branches: [ devr3 ]
    paths:
      - "data-upload/**"
      - "deploy-scripts/**"
      - "manifests/**"
      - ".github/**"
     
jobs:
  Build-Deploy:
    name: R3
    uses: ./.github/workflows/build&deploy.yml
    with:
      module: data-upload
    secrets: inherit




