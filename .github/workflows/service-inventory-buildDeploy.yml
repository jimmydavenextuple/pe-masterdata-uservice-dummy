name: SERVICE-INVENTORY BUILD-DEPLOY

on:
  push:
    branches: [ devr3 ]
    paths:
      - "service-inventory/**"
      - "deploy-scripts/**"
      - "manifests/**"
      - ".github/**"
     
jobs:
  Build-Deploy:
    name: R3
    uses: ./.github/workflows/build&deploy.yml
    with:
      module: service-inventory
    secrets: inherit




