name: TRANSIT BUILD-DEPLOY

on:
  push:
    branches: [ devr3 ]
    paths:
      - "transit/**"
      - "deploy-scripts/**"
      - "manifests/**"
      - ".github/**"
     
jobs:
  Build-Deploy:
    name: R3
    uses: ./.github/workflows/build&deploy.yml
    with:
      module: transit
    secrets: inherit




