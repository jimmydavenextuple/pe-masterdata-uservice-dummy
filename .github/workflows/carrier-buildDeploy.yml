name: CARRIER BUILD-DEPLOY

on:
  push:
    branches: [ development ]
    paths:
      - "carrier/**"
      - "deploy-scripts/**"
      - "manifests/**"
      - ".github/**"
     
jobs:
  Build-Deploy:
    uses: ./.github/workflows/build&deploy.yml
    with:
      module: carrier
    secrets: inherit




