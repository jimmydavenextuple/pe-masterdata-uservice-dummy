name: MASTER-DATA BUILD-DEPLOY

on:
  push:
    branches: [ development ]
    paths:
      - "master-data/**"
jobs:
  Build-Deploy:
    uses: ./.github/workflows/build&deploy.yml
    with:
      module: master-data
    secrets: inherit




