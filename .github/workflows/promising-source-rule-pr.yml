name: PR-PROMISE-SOURCING-RULE

on:
  workflow_call:
  pull_request:
    paths:
      - "promise-sourcing-rule/**"
    branches: [ devr3, hotfix ]

jobs:
  PR-Analysis:
    name: R3
    uses: ./.github/workflows/pr-flow.yml
    with:
      module: promise-sourcing-rule
    secrets: inherit