name: CALENDAR BUILD-DEPLOY

on:
  push:
    branches: [ development ]
    paths:
      - "calendar/**"
jobs:

  Build-Deploy:
    uses: ./.github/workflows/build&deploy.yml
    with:
      module: calendar
    secrets: inherit




