name: DATA-UPLOAD HOTFIX

on:
  push:
    branches: [ hotfix ]
    paths:
      - "data-upload/**"
      - "deploy-scripts/**"
      - "manifests/**"
      - ".github/**"
      - "common-util/**"
      - "build.gradle"
     
jobs:
  Build-Deploy:
    uses: ./.github/workflows/Build&Deploy_Hotfix.yml
    with:
      module: data-upload
    secrets: inherit




