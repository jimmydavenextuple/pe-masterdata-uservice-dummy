name: reusable-workflow

on:
  workflow_call:
    inputs:
      module:
        required: true
        type: string

jobs:
  PR-CHECK:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: 11
        cache: 'gradle'
    - name: PR-BUILD
      run: |
        bash ./gradlew -Pbuild_target=SNAPSHOT clean build jacocoTestReport spotlessApply spotbugsMain -p ${{ inputs.module }}
        bash ./gradlew -Pbuild_target=SNAPSHOT dependencyCheckAggregate -p ${{ inputs.module }} --info
#        aws eks update-kubeconfig --name ${{ secrets.SONAR_CLUSTER }} --region us-east-1
#        kubectl port-forward pod/sonarqube-89-community-0 9000:9000 &
#        bash ./gradlew sonarqube -Dsonar.pullrequest.branch=${{ github.head_ref }} -Dsonar.pullrequest.base=${{ github.base_ref }} -Dsonar.pullrequest.key=${{ github.event.number }} showSonarQubeLink --info
#        fuser -k 9000/tcp
    - name: Publish Test Results
      uses: EnricoMi/publish-unit-test-result-action@v1
      if: always()
      with:
        files: "**/**/test-results/**/*.xml"