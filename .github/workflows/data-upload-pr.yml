name: PR-DATA-UPLOAD

on:
  workflow_call:
  pull_request:
    paths:
      - "data-upload/**"
    branches: [ devr3, hotfix ]

jobs:
  PR-Analysis:
    name: R3
    uses: ./.github/workflows/pr-flow.yml
    with:
      module: data-upload
    secrets: inherit