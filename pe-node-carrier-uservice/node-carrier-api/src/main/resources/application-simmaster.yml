spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: \${POSTGRES-URI}
    username: \${COMMON-POSTGRES-USER}
    password: \${COMMON-POSTGRES-PASSWORD}
  kafka:
    bootstrap-servers: \${KAFKA-BROKER-URI}
    properties:
      security.protocol: SASL_PLAINTEXT
      sasl.mechanism: SCRAM-SHA-256
      sasl.jaas.config: \${COMMON-KAFKA-CONFIG}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      topics:
        master_data_update_event: INTERNAL.SIMMASTER.MASTER_DATA_UPDATE_EVENTS
      properties:
        acks: 1
        retries: 5
        retry.backoff.ms: 1000
        spring.json.trusted.packages: "*"
        interceptor.classes: "com.nextuple.common.interceptor.KafkaProducerContextInterceptor"
  sql.init.platform: postgres

logging:
  level:
    com.nextuple: INFO

master-data:
  node-carrier:
    topic-name: INTERNAL.SIMMASTER.PE.NODE_CARRIER_INTEGRATION
    group-id: node-carrier-master-simmaster
  processing-lead-time:
    topic-name: INTERNAL.SIMMASTER.PE.PROCESSING_LEAD_TIME_INTEGRATION
    group-id: processing-lead-time-master-simmaster
  node-service-option-buffer:
    topic-name: INTERNAL.SIMMASTER.PE.NODE_SERVICE_OPTION_BUFFER_INTEGRATION
    group-id: node-service-option-buffer-master-simmaster