bootJar {
    enabled = true
    mainClass.set("com.hbc.csvdownload.CsvUtilityApplication")
}
dependencies {
    implementation(project(":csv-utility:csv-download-common-util"))
    runtimeOnly 'org.postgresql:postgresql'
    implementation(project(":common-util"))
}

test {
    useJUnitPlatform()
}