plugins {
    id 'io.spring.dependency-management' version '1.1.4'
}

jar.enabled=true

bootJar {
    enabled = false
}

dependencies {
    implementation(project(":postal-code-timezone-api-domain"))
    implementation("com.nextuple.pe:common-util:1.3.0-${build_target}")
    implementation("com.nextuple.pe:core-near-cache:1.3.0-${build_target}")
}
tasks.withType(Test).configureEach {
    useJUnitPlatform()
}

tasks.named('test') {
    dependsOn(
            ':postal-code-timezone-near-cache:jar',
            ':postal-code-timezone-api-domain:jar'
    )
}