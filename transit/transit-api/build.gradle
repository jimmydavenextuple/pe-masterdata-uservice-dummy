bootJar {
    enabled = true
    mainClass.set("com.hbc.transit.MasterDataTransitApplication")
}

configurations {
    [apiElements, runtimeElements].each {
        it.outgoing.artifacts.removeIf { it.buildDependencies.getDependencies(null).contains(jar) }
        it.outgoing.artifact(bootJar)
    }
}

dependencies {
    implementation(project(":transit:transit-api-domain"))
    runtimeOnly 'org.postgresql:postgresql'
    implementation(project(":common-util"))
    implementation project(":common:common-postgres")
    implementation(project(":nfs-near-cache:core-near-cache-producer"))
    implementation(project(":carrier:carrier-api"))
    implementation(project(":carrier:carrier-api-domain"))
}
sourceCompatibility = JavaVersion.VERSION_11
targetCompatibility = JavaVersion.VERSION_11
